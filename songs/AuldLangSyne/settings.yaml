# Different voice options [:np] [:nb] [:nh] [:nd] [:nf] [:nu] [:nr] [:nw] [:nk]

# How much to shift pitches by (DECTALK Pitches not the same as midi, should be 48 off or so)
noteOffset: -52

# Words are broken up into phonemes, the playback lengths of which must be specified in code
# This is calculated by setting a standard fraction of each word to dedicate to consonants
# Also specify min and max length in milliseconds for each consonant to prevent weird issues
consonantFractionTarget: 0.25
consonantMinMs: 5
consonantMaxMs: 100

# Gaps at or below this threshold are folded into the previous note instead of emitted as rests.
# Tracks can override this with GAP_MEND_MS.
gapMendMs: 20

# Set Specific settings for each track
# Make sure each track is listed here, included in the MIDI file, and has a .txt file in lyrics all with the same title
Tracks:
  Soprano:
    DEC_SETUP: "[:np][:dv hs 95]"
    VOLUME_ADJUST_DB: 6

    SPECTROGRAM:
      COLOR_HSB: [328, 70, 97]
      POSITION: [0.5, 0, 0]
      LABEL: "Soprano"
      LABEL_ENABLED: true
      LABEL_POSITION: "top-left"
      LABEL_SHOW_VOICE: true
      LABEL_SHOW_HEAD_SIZE: true
      LABEL_FONT: "serif"
      LABEL_FONT_SIZE_PERCENT: 7
      CURRENT_WORD_ENABLED: true
      CURRENT_WORD_POSITION: "bottom-center"
      CURRENT_WORD_FONT: "mono"
      CURRENT_WORD_FONT_SIZE_PERCENT: 10
      CURRENT_WORD_USE_TRACK_COLOR: true

    PITCH_SHIFT: 0
    OCTAVE_BOOST: 0
    PITCH_WRAP_SHIFT: null
    IGNORE_MIDI_VELOCITY: true
    VELOCITY_VOLUME_SCALE_DB: 0
    STEM_PEAK_CEILING_DBFS: -2.5
    GAP_MEND_MS: 20
    MINIMUM_NOTE_DURATION_MS: 0
    CODA_MAX_MS: 200
  Alto:
    DEC_SETUP: "[:nk][:dv hs 100]"
    VOLUME_ADJUST_DB: 0.0

    SPECTROGRAM:
      COLOR_HSB: [347, 79, 100]
      POSITION: [0.5, 0.5, 0]
      LABEL: "Alto"
      LABEL_ENABLED: true
      LABEL_POSITION: "top-left"
      LABEL_SHOW_VOICE: true
      LABEL_SHOW_HEAD_SIZE: true
      LABEL_FONT: "serif"
      LABEL_FONT_SIZE_PERCENT: 7
      CURRENT_WORD_ENABLED: true
      CURRENT_WORD_POSITION: "bottom-center"
      CURRENT_WORD_FONT: "mono"
      CURRENT_WORD_FONT_SIZE_PERCENT: 10
      CURRENT_WORD_USE_TRACK_COLOR: true

  Tenor:
    DEC_SETUP: "[:np][:dv hs 110]"
    VOLUME_ADJUST_DB: -5.0

    SPECTROGRAM:
      COLOR_HSB: [322, 59, 65]
      POSITION: [0.5, 0.5, 0.5]
      LABEL: "Tenor"
      LABEL_ENABLED: true
      LABEL_POSITION: "top-left"
      LABEL_SHOW_VOICE: true
      LABEL_SHOW_HEAD_SIZE: true
      LABEL_FONT: "serif"
      LABEL_FONT_SIZE_PERCENT: 7
      CURRENT_WORD_ENABLED: true
      CURRENT_WORD_POSITION: "bottom-center"
      CURRENT_WORD_FONT: "mono"
      CURRENT_WORD_FONT_SIZE_PERCENT: 10
      CURRENT_WORD_USE_TRACK_COLOR: true

  Bass:
    DEC_SETUP: "[:np][:dv hs 120]"
    VOLUME_ADJUST_DB: -10.0

    SPECTROGRAM:
      COLOR_HSB: [214, 49, 100]
      POSITION: [0.5, 0, 0.5]
      LABEL: "Bass"
      LABEL_ENABLED: true
      LABEL_POSITION: "top-left"
      LABEL_SHOW_VOICE: true
      LABEL_SHOW_HEAD_SIZE: true
      LABEL_FONT: "serif"
      LABEL_FONT_SIZE_PERCENT: 7
      CURRENT_WORD_ENABLED: true
      CURRENT_WORD_POSITION: "bottom-center"
      CURRENT_WORD_FONT: "mono"
      CURRENT_WORD_FONT_SIZE_PERCENT: 10
      CURRENT_WORD_USE_TRACK_COLOR: true

